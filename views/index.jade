extends layout

block content
  
  header
    .container
      h1
        | Sup homies
      form(ng-submit="submitUsername(desiredUsername)")
        .input-group
          div(ng-hide="!error")
            |{{error}}
          input.usernameEntry(ng-model="desiredUsername" type="text" placeholder="Enter username here")
          button.usernameSubmit(type="submit" value="Submit") Submit
  div.content
